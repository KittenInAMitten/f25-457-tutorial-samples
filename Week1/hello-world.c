#include <stdio.h>

/**
 * @brief This program is super simple and will be used to show you how to compile your very first C program!
 * 
 * Ensure you are logged into/SSH'd into the cslinux servers/computers.
 * 
 *  1. Complete the main function using printf().
 *  2. Once done, type into the terminal: `gcc -O2 -Wall hello-world.c -o hello-world`
 *  3. Type in `./hello-world`
 *  4. Confirm the output says "hello world" or whatever you chose to output in the terminal
 * 
 * @return int 
 */
int main(){

    // Just print "hello world" or whatever you want :)
    return 0;
}